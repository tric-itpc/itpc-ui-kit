import {
  Link,
  LinkProps,
  Text,
  TextProps,
  Title,
  TitleProps,
} from "./components"

export const Typography = {
  Link,
  Text,
  Title,
}

export type { LinkProps, TextProps, TitleProps }

import { Orientation } from "../../types";
interface Props {
    isClicable?: boolean;
    orientation?: Orientation;
}
export declare const Icon: import("styled-components").StyledComponent<"i", any, Props, never>;
export {};

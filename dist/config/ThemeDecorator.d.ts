import React from "react";
import { Story } from "@storybook/react";
import { Theme } from "../enums";
export declare const ThemeDecorator: (theme: Theme) => (StoryComponent: Story) => React.JSX.Element;
